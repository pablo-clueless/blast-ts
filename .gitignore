# Rust
/target
STRATEGY.md

# Environment
.env
.env.local

# Editor
.vscode/
.idea/
*.swp

# OS
.DS_Store
# Added by goreleaser init:
dist/
.intentionally-empty-file.o
target/

# Node / NAPI-RS
node_modules/
# Compiled native addons — shipped via per-platform npm packages, never committed
*.node
# NOTE: index.js (loader) and index.d.ts (types) are generated by `napi build`
# but ARE committed — the publish job needs them and does not compile Rust.
# Regenerate and commit them whenever the NAPI bindings change.
